{
  "name": "Gleam Development Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true,
        "[gleam]": {
          "editor.defaultFormatter": "gleam.gleam"
        }
      },
      "extensions": [
        "gleam.gleam",
        "github.copilot",
        "github.copilot-chat"
      ]
    }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y erlang-dev erlang-parsetools curl && curl -L -o gleam.tar.gz https://github.com/gleam-lang/gleam/releases/download/v1.3.1/gleam-v1.3.1-x86_64-unknown-linux-musl.tar.gz && sudo tar -xzf gleam.tar.gz -C /usr/local/bin && rm gleam.tar.gz && gleam deps download",

  "forwardPorts": [4000]
}
